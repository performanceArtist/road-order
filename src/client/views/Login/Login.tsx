import * as React from 'react';

import Keyboard from '@components/Keyboard/Keyboard';

const Login = () => {
  return (
    <div>
      <Keyboard />
    </div>
  );
};

export default Login;
