import * as React from 'react';

import TaskPanel from '../../features/Task/TaskPanel';

const TaskView = () => {
  return (
    <div>
      <TaskPanel />
    </div>
  );
};

export default TaskView;
