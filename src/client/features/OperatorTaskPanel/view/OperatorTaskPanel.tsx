import React from 'react';

import { TaskPanel } from '@features/TaskPanel';

const OperatorTaskPanel = () => {
  return (
    <div className="operator-task-panel">
      <TaskPanel />
    </div>
  );
};

export default OperatorTaskPanel;
